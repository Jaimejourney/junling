package com.junling.mis.mapper.primary;

import com.junling.mis.model.primary.drugsHenanEntity;

public interface drugsHenanEntityMapper {
    int insert(drugsHenanEntity record);

    int insertSelective(drugsHenanEntity record);
}